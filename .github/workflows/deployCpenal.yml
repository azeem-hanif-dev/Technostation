# name: Publish Website to CPanel

# on:
#   push:
#     branches:
#       - main

# jobs:
#   FTP-Deploy-Action:
#     name: FTP-Deploy-Action
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 2  # Allows comparing with the last commit

#       - name: Get Changed Files
#         id: changed-files
#         run: |
#           echo "CHANGED_FILES=$(git diff --name-only HEAD~1 | tr '\n' ' ')" >> $GITHUB_ENV

#       - name: Install Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '10'

#       - name: Install Dependencies
#         run: npm install

#       - name: Build Assets
#         run: npm run production

#       - name: Install PHP and Composer
#         uses: shivammathur/setup-php@v2
#         with:
#           php-version: '7.4'
#           tools: 'composer'

#       - name: Install PHP Dependencies
#         run: composer install --no-dev --optimize-autoloader
      
#       - name: Clear and Cache Laravel Config
#         run: php artisan config:clear && php artisan config:cache

#       - name: Composer Autoload 
#         run: composer dump-autoload

#       - name: FTP Deploy (Only Changed Files)
#         if: env.CHANGED_FILES != ''
#         uses: SamKirkland/FTP-Deploy-Action@4.3.3
#         with:
#           server: ${{ secrets.FTP_SERVER1 }}
#           username: ${{ secrets.FTP_USERNAME1 }}
#           password: ${{ secrets.FTP_PASSWORD1 }}
#           protocol: ftp
#           port: 21
#           local-dir: ./
#           server-dir: /domains/technostation.org/public_html/aa/
#           concurrency: 10
#           only-newer: true  # ✅ Upload only modified files
#           dangerous-clean-slate: false  # ✅ Prevents deletion of existing files
#           exclude: |
#             **/.git*
#             **/node_modules/**
#             **/storage/**
#             **/vendor/**
#             **/.env
#             **/.github/
